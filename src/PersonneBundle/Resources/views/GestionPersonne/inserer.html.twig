<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <title>Connexion</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
<link href='http://fonts.googleapis.com/css?family=Varela+Round' rel='stylesheet' type='text/css'>
<style>
body{
    background: url('{{ asset("bundles/personne/images/amat2.jpg")}}') no-repeat center fixed;
    }

</style>
</head>
<body>
<div class="container">
<div style="margin-top:80px;" class="mainbox col-md-5  col-sm-7 ">                    
<div class="panel panel-info" >
<div class="panel-heading">
<div class="panel-title"><span class="glyphicon glyphicon-user"></span></div>
</div>
<div style="padding-top:30px ; padding-left: 40px ;" class="panel-body"  >
<div class="row">

{% form_theme form 'bootstrap_3_layout.html.twig' %}

 {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}

  {# Les erreurs générales du formulaire. #}
  {{ form_errors(form) }}

  {# Génération du label + error + widget pour un champ. #}

  {# Génération manuelle et éclatée : #}
  <div class="form-group">
    {# Génération du label. #}
   

    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.nom) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.nom, {'attr': {'class': 'form-control' , 'placeholder': 'Votre nom'}}) }}
    </div>
  </div>
  <div class="form-group">
    {# Génération du label. #}
  

    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.prenom) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.prenom, {'attr': {'class': 'form-control' , 'placeholder': 'Votre prenom'}}) }}
    </div>
  </div>
  <div class="form-group">
    {# Génération du label. #}
    

    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.email) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.email, {'attr': {'class': 'form-control', 'placeholder': 'Votre email'}}) }}
    </div>
    </div>
    <div class="form-group">
    {# Génération du label. #}
    
    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.password) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.password, {'attr': {'class': 'form-control', 'placeholder': 'Votre password'}}) }}
    </div>
    </div>
    <div class="form-group">
    {# Génération du label. #}
   

    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.sexe) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.sexe, {'attr': {'class': 'form-control' , 'placeholder': 'votre sexe'}}) }}
    </div>
    </div>
    <div class="form-group">
    {# Génération du label. #}
   

    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.numero) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.numero, {'attr': {'class': 'form-control' , 'placeholder': 'Votre numero de rue'}}) }}
    </div>
    </div>
    <div class="form-group">
    {# Génération du label. #}
   
    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.rue) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.rue, {'attr': {'class': 'form-control' , 'placeholder': 'votre rue'}}) }}
    </div>
  </div>
   <div class="form-group">
    {# Génération du label. #}
    
    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.codePostal) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.codePostal, {'attr': {'class': 'form-control' , 'placeholder': 'votre code postal'}}) }}
    </div>
  </div>
   <div class="form-group">
    {# Génération du label. #}
  

    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.ville) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.ville, {'attr': {'class': 'form-control' , 'placeholder': 'votre ville'}}) }}
    </div>
  </div>
  <div class="form-group">
    {# Génération du label. #}
  

    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.photo) }}

    <div class="col-sm-10">
      {# Génération de l'input. #}
      {{ form_widget(form.photo, {'attr': {'class': 'form-control' , 'placeholder': 'votre photo'}}) }}
    </div>
  </div>
  <div>
    {{ form_widget(form.enregistrer, {'attr' : {'class' : 'btn-group btn-group-lg btn-info', 'role' : 'group'}}) }} <a class = "btn-group btn-group-lg btn-info" role="group" href= {{ path('connexion')}} ><span class="glyphicon glyphicon-remove"></span> </a>
  </div>
  {{ form_end(form) }}
  </div>
  </div>
</body>
</html>